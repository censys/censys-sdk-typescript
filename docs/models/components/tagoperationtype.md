# TagOperationType

Whether the operation creates or deletes tag assignments.

## Example Usage

```typescript
import { TagOperationType } from "@censys/platform-sdk/models/components";

let value: TagOperationType = "bulk_delete";

// Open enum: unrecognized values are captured as Unrecognized<string>
```

## Values

```typescript
"bulk_create" | "bulk_delete" | Unrecognized<string>
```