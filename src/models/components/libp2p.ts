/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type Libp2P = {
  advertisedProtocols?: Array<string> | null | undefined;
  multistreamVersion?: string | undefined;
};

/** @internal */
export const Libp2P$inboundSchema: z.ZodType<Libp2P, z.ZodTypeDef, unknown> = z
  .object({
    advertised_protocols: z.nullable(z.array(z.string())).optional(),
    multistream_version: z.string().optional(),
  }).transform((v) => {
    return remap$(v, {
      "advertised_protocols": "advertisedProtocols",
      "multistream_version": "multistreamVersion",
    });
  });

export function libp2PFromJSON(
  jsonString: string,
): SafeParseResult<Libp2P, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Libp2P$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Libp2P' from JSON`,
  );
}
