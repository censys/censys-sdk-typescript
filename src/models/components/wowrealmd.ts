/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type WowRealmd = {
  responseType?: string | undefined;
  resultName?: string | undefined;
};

/** @internal */
export const WowRealmd$inboundSchema: z.ZodType<
  WowRealmd,
  z.ZodTypeDef,
  unknown
> = z.object({
  response_type: z.string().optional(),
  result_name: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    "response_type": "responseType",
    "result_name": "resultName",
  });
});

export function wowRealmdFromJSON(
  jsonString: string,
): SafeParseResult<WowRealmd, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => WowRealmd$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'WowRealmd' from JSON`,
  );
}
