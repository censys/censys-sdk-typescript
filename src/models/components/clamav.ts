/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type Clamav = {
  engineVersion?: string | undefined;
  signatureDate?: string | undefined;
  signatureVersion?: number | undefined;
};

/** @internal */
export const Clamav$inboundSchema: z.ZodType<Clamav, z.ZodTypeDef, unknown> = z
  .object({
    engine_version: z.string().optional(),
    signature_date: z.string().optional(),
    signature_version: z.number().int().optional(),
  }).transform((v) => {
    return remap$(v, {
      "engine_version": "engineVersion",
      "signature_date": "signatureDate",
      "signature_version": "signatureVersion",
    });
  });

export function clamavFromJSON(
  jsonString: string,
): SafeParseResult<Clamav, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Clamav$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Clamav' from JSON`,
  );
}
