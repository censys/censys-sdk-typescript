/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  DnsIpResolutionRecord,
  DnsIpResolutionRecord$inboundSchema,
} from "./dnsipresolutionrecord.js";

export type DnsIpResolutionBoundResponse = {
  /**
   * The IP address that was queried.
   */
  ip: string;
  /**
   * A token that can be used to retrieve the next page of records.
   */
  nextPageToken: string;
  /**
   * The list of domain names that resolved to this IP.
   */
  records: Array<DnsIpResolutionRecord> | null;
  /**
   * The number of records that exist in total across all pages.
   */
  totalRecords: number;
};

/** @internal */
export const DnsIpResolutionBoundResponse$inboundSchema: z.ZodType<
  DnsIpResolutionBoundResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  ip: z.string(),
  next_page_token: z.string(),
  records: z.nullable(z.array(DnsIpResolutionRecord$inboundSchema)),
  total_records: z.number().int(),
}).transform((v) => {
  return remap$(v, {
    "next_page_token": "nextPageToken",
    "total_records": "totalRecords",
  });
});

export function dnsIpResolutionBoundResponseFromJSON(
  jsonString: string,
): SafeParseResult<DnsIpResolutionBoundResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DnsIpResolutionBoundResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DnsIpResolutionBoundResponse' from JSON`,
  );
}
