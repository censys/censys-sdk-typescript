/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type Stomp = {
  errorMessage?: string | undefined;
  frameType?: string | undefined;
  protocolVersion?: string | undefined;
  server?: string | undefined;
  session?: string | undefined;
};

/** @internal */
export const Stomp$inboundSchema: z.ZodType<Stomp, z.ZodTypeDef, unknown> = z
  .object({
    error_message: z.string().optional(),
    frame_type: z.string().optional(),
    protocol_version: z.string().optional(),
    server: z.string().optional(),
    session: z.string().optional(),
  }).transform((v) => {
    return remap$(v, {
      "error_message": "errorMessage",
      "frame_type": "frameType",
      "protocol_version": "protocolVersion",
    });
  });

export function stompFromJSON(
  jsonString: string,
): SafeParseResult<Stomp, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Stomp$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Stomp' from JSON`,
  );
}
