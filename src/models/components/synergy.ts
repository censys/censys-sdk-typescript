/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type Synergy = {
  protocolMajor?: number | undefined;
  protocolMinor?: number | undefined;
  protocolVersion?: string | undefined;
};

/** @internal */
export const Synergy$inboundSchema: z.ZodType<Synergy, z.ZodTypeDef, unknown> =
  z.object({
    protocol_major: z.number().int().optional(),
    protocol_minor: z.number().int().optional(),
    protocol_version: z.string().optional(),
  }).transform((v) => {
    return remap$(v, {
      "protocol_major": "protocolMajor",
      "protocol_minor": "protocolMinor",
      "protocol_version": "protocolVersion",
    });
  });

export function synergyFromJSON(
  jsonString: string,
): SafeParseResult<Synergy, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Synergy$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Synergy' from JSON`,
  );
}
