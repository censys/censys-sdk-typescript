/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type GopherItem = {
  displayString?: string | undefined;
  hostname?: string | undefined;
  itemType?: string | undefined;
  port?: number | undefined;
  selector?: string | undefined;
};

/** @internal */
export const GopherItem$inboundSchema: z.ZodType<
  GopherItem,
  z.ZodTypeDef,
  unknown
> = z.object({
  display_string: z.string().optional(),
  hostname: z.string().optional(),
  item_type: z.string().optional(),
  port: z.number().int().optional(),
  selector: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    "display_string": "displayString",
    "item_type": "itemType",
  });
});

export function gopherItemFromJSON(
  jsonString: string,
): SafeParseResult<GopherItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GopherItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GopherItem' from JSON`,
  );
}
