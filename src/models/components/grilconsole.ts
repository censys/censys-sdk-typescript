/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type GrilConsole = {
  board?: string | undefined;
  firmwareVersion?: string | undefined;
  model?: string | undefined;
  receiverId?: string | undefined;
  vendor?: string | undefined;
};

/** @internal */
export const GrilConsole$inboundSchema: z.ZodType<
  GrilConsole,
  z.ZodTypeDef,
  unknown
> = z.object({
  board: z.string().optional(),
  firmware_version: z.string().optional(),
  model: z.string().optional(),
  receiver_id: z.string().optional(),
  vendor: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    "firmware_version": "firmwareVersion",
    "receiver_id": "receiverId",
  });
});

export function grilConsoleFromJSON(
  jsonString: string,
): SafeParseResult<GrilConsole, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GrilConsole$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GrilConsole' from JSON`,
  );
}
