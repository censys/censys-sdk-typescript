/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  GiopIiopProfile,
  GiopIiopProfile$inboundSchema,
} from "./giopiiopprofile.js";

export type GiopIor = {
  iiopProfile?: GiopIiopProfile | undefined;
  typeId?: string | undefined;
};

/** @internal */
export const GiopIor$inboundSchema: z.ZodType<GiopIor, z.ZodTypeDef, unknown> =
  z.object({
    iiop_profile: GiopIiopProfile$inboundSchema.optional(),
    type_id: z.string().optional(),
  }).transform((v) => {
    return remap$(v, {
      "iiop_profile": "iiopProfile",
      "type_id": "typeId",
    });
  });

export function giopIorFromJSON(
  jsonString: string,
): SafeParseResult<GiopIor, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GiopIor$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GiopIor' from JSON`,
  );
}
