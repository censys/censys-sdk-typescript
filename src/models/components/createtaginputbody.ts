/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { ClosedEnum } from "../../types/enums.js";

/**
 * Tag visibility and management settings. `private` tags are only visible to and editable by organization admins. `shared` tags are visible to and editable by all organization members.
 */
export const CreateTagInputBodyPrivacy = {
  Private: "private",
  Shared: "shared",
} as const;
/**
 * Tag visibility and management settings. `private` tags are only visible to and editable by organization admins. `shared` tags are visible to and editable by all organization members.
 */
export type CreateTagInputBodyPrivacy = ClosedEnum<
  typeof CreateTagInputBodyPrivacy
>;

export type CreateTagInputBody = {
  /**
   * A human-readable description of the tag.
   */
  description?: string | undefined;
  /**
   * The display name of the tag. Must be unique within the organization.
   */
  name: string;
  /**
   * Tag visibility and management settings. `private` tags are only visible to and editable by organization admins. `shared` tags are visible to and editable by all organization members.
   */
  privacy: CreateTagInputBodyPrivacy;
};

/** @internal */
export const CreateTagInputBodyPrivacy$outboundSchema: z.ZodNativeEnum<
  typeof CreateTagInputBodyPrivacy
> = z.nativeEnum(CreateTagInputBodyPrivacy);

/** @internal */
export type CreateTagInputBody$Outbound = {
  description?: string | undefined;
  name: string;
  privacy: string;
};

/** @internal */
export const CreateTagInputBody$outboundSchema: z.ZodType<
  CreateTagInputBody$Outbound,
  z.ZodTypeDef,
  CreateTagInputBody
> = z.object({
  description: z.string().optional(),
  name: z.string(),
  privacy: CreateTagInputBodyPrivacy$outboundSchema,
});

export function createTagInputBodyToJSON(
  createTagInputBody: CreateTagInputBody,
): string {
  return JSON.stringify(
    CreateTagInputBody$outboundSchema.parse(createTagInputBody),
  );
}
