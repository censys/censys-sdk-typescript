/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type Wordpress = {
  description?: string | undefined;
  gmtOffset?: string | undefined;
  home?: string | undefined;
  name?: string | undefined;
  namespaces?: Array<string> | null | undefined;
  timezoneString?: string | undefined;
};

/** @internal */
export const Wordpress$inboundSchema: z.ZodType<
  Wordpress,
  z.ZodTypeDef,
  unknown
> = z.object({
  description: z.string().optional(),
  gmt_offset: z.string().optional(),
  home: z.string().optional(),
  name: z.string().optional(),
  namespaces: z.nullable(z.array(z.string())).optional(),
  timezone_string: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    "gmt_offset": "gmtOffset",
    "timezone_string": "timezoneString",
  });
});

export function wordpressFromJSON(
  jsonString: string,
): SafeParseResult<Wordpress, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Wordpress$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Wordpress' from JSON`,
  );
}
