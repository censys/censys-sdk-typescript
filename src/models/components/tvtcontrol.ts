/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type TvtControl = {
  macAddress?: string | undefined;
  vendor?: string | undefined;
};

/** @internal */
export const TvtControl$inboundSchema: z.ZodType<
  TvtControl,
  z.ZodTypeDef,
  unknown
> = z.object({
  mac_address: z.string().optional(),
  vendor: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    "mac_address": "macAddress",
  });
});

export function tvtControlFromJSON(
  jsonString: string,
): SafeParseResult<TvtControl, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TvtControl$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TvtControl' from JSON`,
  );
}
