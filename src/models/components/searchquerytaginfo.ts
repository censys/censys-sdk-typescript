/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type SearchQueryTagInfo = {
  /**
   * Name of the tag.
   */
  displayName: string;
  /**
   * ID of the tag.
   */
  id?: string | undefined;
};

/** @internal */
export const SearchQueryTagInfo$inboundSchema: z.ZodType<
  SearchQueryTagInfo,
  z.ZodTypeDef,
  unknown
> = z.object({
  display_name: z.string(),
  id: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    "display_name": "displayName",
  });
});

export function searchQueryTagInfoFromJSON(
  jsonString: string,
): SafeParseResult<SearchQueryTagInfo, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SearchQueryTagInfo$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SearchQueryTagInfo' from JSON`,
  );
}
