/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import * as openEnums from "../../types/enums.js";
import { OpenEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * The inferred type of the asset.
 */
export const AssetType = {
  Host: "host",
  Certificate: "certificate",
  WebProperty: "web_property",
  Unknown: "unknown",
} as const;
/**
 * The inferred type of the asset.
 */
export type AssetType = OpenEnum<typeof AssetType>;

export type Comment = {
  /**
   * The identifier of the commented asset (host IP, certificate SHA-256 fingerprint, or web property domain/IP:port).
   */
  assetId: string;
  /**
   * The inferred type of the asset.
   */
  assetType: AssetType;
  /**
   * The comment body text.
   */
  body: string;
  /**
   * RFC3339 timestamp when the comment was created.
   */
  createdAt: Date;
  /**
   * The user ID of the user who created the comment.
   */
  createdBy: string;
  /**
   * The unique ID of the comment.
   */
  id: string;
  /**
   * A direct URL to the asset in the Censys platform.
   */
  platformRef: string;
  /**
   * RFC3339 timestamp when the comment was last updated.
   */
  updatedAt: Date;
};

/** @internal */
export const AssetType$inboundSchema: z.ZodType<
  AssetType,
  z.ZodTypeDef,
  unknown
> = openEnums.inboundSchema(AssetType);

/** @internal */
export const Comment$inboundSchema: z.ZodType<Comment, z.ZodTypeDef, unknown> =
  z.object({
    asset_id: z.string(),
    asset_type: AssetType$inboundSchema,
    body: z.string(),
    created_at: z.string().datetime({ offset: true }).transform(v =>
      new Date(v)
    ),
    created_by: z.string(),
    id: z.string(),
    platform_ref: z.string(),
    updated_at: z.string().datetime({ offset: true }).transform(v =>
      new Date(v)
    ),
  }).transform((v) => {
    return remap$(v, {
      "asset_id": "assetId",
      "asset_type": "assetType",
      "created_at": "createdAt",
      "created_by": "createdBy",
      "platform_ref": "platformRef",
      "updated_at": "updatedAt",
    });
  });

export function commentFromJSON(
  jsonString: string,
): SafeParseResult<Comment, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Comment$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Comment' from JSON`,
  );
}
