/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import * as openEnums from "../../types/enums.js";
import { OpenEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * The record type. Either A, AAAA, MX, NS, SOA, or TXT.
 */
export const DnsResolutionRecordRecordType = {
  A: "A",
  Aaaa: "AAAA",
  Mx: "MX",
  Ns: "NS",
  Soa: "SOA",
  Txt: "TXT",
} as const;
/**
 * The record type. Either A, AAAA, MX, NS, SOA, or TXT.
 */
export type DnsResolutionRecordRecordType = OpenEnum<
  typeof DnsResolutionRecordRecordType
>;

export type DnsResolutionRecord = {
  /**
   * The time this record was first observed.
   */
  firstSeen: Date;
  /**
   * The IP of the record. IPv4 if the record_type is A. IPv6 if the record_type is AAAA. Otherwise not present.
   */
  ip?: string | undefined;
  /**
   * The time this record was last observed.
   */
  lastSeen: Date;
  /**
   * The MX mail server. Only present when record_type is MX. Otherwise not present.
   */
  mailServer?: string | undefined;
  /**
   * The primary name server. Only present when record_type is SOA. Otherwise not present.
   */
  mname?: string | undefined;
  /**
   * The name server. Only present when record_type is NS. Otherwise not present.
   */
  nameServer?: string | undefined;
  /**
   * The MX priority. Only present when record_type is MX. Otherwise not present.
   */
  priority?: number | undefined;
  /**
   * The record type. Either A, AAAA, MX, NS, SOA, or TXT.
   */
  recordType: DnsResolutionRecordRecordType;
  /**
   * The responsible contact, as email. Only present when record_type is SOA. Otherwise not present.
   */
  rname?: string | undefined;
  /**
   * Full, untruncated string. Only present when record_type is TXT. Otherwise not present.
   */
  value?: string | undefined;
};

/** @internal */
export const DnsResolutionRecordRecordType$inboundSchema: z.ZodType<
  DnsResolutionRecordRecordType,
  z.ZodTypeDef,
  unknown
> = openEnums.inboundSchema(DnsResolutionRecordRecordType);

/** @internal */
export const DnsResolutionRecord$inboundSchema: z.ZodType<
  DnsResolutionRecord,
  z.ZodTypeDef,
  unknown
> = z.object({
  first_seen: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  ip: z.string().optional(),
  last_seen: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  mail_server: z.string().optional(),
  mname: z.string().optional(),
  name_server: z.string().optional(),
  priority: z.number().int().optional(),
  record_type: DnsResolutionRecordRecordType$inboundSchema,
  rname: z.string().optional(),
  value: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    "first_seen": "firstSeen",
    "last_seen": "lastSeen",
    "mail_server": "mailServer",
    "name_server": "nameServer",
    "record_type": "recordType",
  });
});

export function dnsResolutionRecordFromJSON(
  jsonString: string,
): SafeParseResult<DnsResolutionRecord, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DnsResolutionRecord$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DnsResolutionRecord' from JSON`,
  );
}
