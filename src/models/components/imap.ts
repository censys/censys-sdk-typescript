/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type Imap = {
  /**
   * The server's response to the STARTTLS command.
   */
  startTls?: string | undefined;
};

/** @internal */
export const Imap$inboundSchema: z.ZodType<Imap, z.ZodTypeDef, unknown> = z
  .object({
    start_tls: z.string().optional(),
  }).transform((v) => {
    return remap$(v, {
      "start_tls": "startTls",
    });
  });

export function imapFromJSON(
  jsonString: string,
): SafeParseResult<Imap, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Imap$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Imap' from JSON`,
  );
}
