/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type ElasticsearchTransport = {
  handshakeOk?: boolean | undefined;
  protocolVersion?: number | undefined;
  transportVersionId?: number | undefined;
  version?: string | undefined;
};

/** @internal */
export const ElasticsearchTransport$inboundSchema: z.ZodType<
  ElasticsearchTransport,
  z.ZodTypeDef,
  unknown
> = z.object({
  handshake_ok: z.boolean().optional(),
  protocol_version: z.number().int().optional(),
  transport_version_id: z.number().int().optional(),
  version: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    "handshake_ok": "handshakeOk",
    "protocol_version": "protocolVersion",
    "transport_version_id": "transportVersionId",
  });
});

export function elasticsearchTransportFromJSON(
  jsonString: string,
): SafeParseResult<ElasticsearchTransport, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ElasticsearchTransport$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ElasticsearchTransport' from JSON`,
  );
}
