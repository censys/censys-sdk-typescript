/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { TagAssignment, TagAssignment$inboundSchema } from "./tagassignment.js";

export type TagAssignmentsList = {
  /**
   * The list of tag assignments.
   */
  assignments: Array<TagAssignment> | null;
  /**
   * Token to retrieve the next page of results. Omitted when there are no more results.
   */
  nextPageToken?: string | undefined;
  /**
   * Total number of assignments matching the filters.
   */
  totalSize: number;
};

/** @internal */
export const TagAssignmentsList$inboundSchema: z.ZodType<
  TagAssignmentsList,
  z.ZodTypeDef,
  unknown
> = z.object({
  assignments: z.nullable(z.array(TagAssignment$inboundSchema)),
  next_page_token: z.string().optional(),
  total_size: z.number().int(),
}).transform((v) => {
  return remap$(v, {
    "next_page_token": "nextPageToken",
    "total_size": "totalSize",
  });
});

export function tagAssignmentsListFromJSON(
  jsonString: string,
): SafeParseResult<TagAssignmentsList, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TagAssignmentsList$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TagAssignmentsList' from JSON`,
  );
}
