/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { Comment, Comment$inboundSchema } from "./comment.js";

export type CommentsList = {
  /**
   * The list of comments.
   */
  comments: Array<Comment> | null;
  /**
   * Token to retrieve the next page of results. Omitted when there are no more results.
   */
  nextPageToken?: string | undefined;
  /**
   * Total number of comments matching the filters.
   */
  totalSize: number;
};

/** @internal */
export const CommentsList$inboundSchema: z.ZodType<
  CommentsList,
  z.ZodTypeDef,
  unknown
> = z.object({
  comments: z.nullable(z.array(Comment$inboundSchema)),
  next_page_token: z.string().optional(),
  total_size: z.number().int(),
}).transform((v) => {
  return remap$(v, {
    "next_page_token": "nextPageToken",
    "total_size": "totalSize",
  });
});

export function commentsListFromJSON(
  jsonString: string,
): SafeParseResult<CommentsList, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CommentsList$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CommentsList' from JSON`,
  );
}
