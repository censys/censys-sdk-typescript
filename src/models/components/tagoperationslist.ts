/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { TagOperation, TagOperation$inboundSchema } from "./tagoperation.js";

export type TagOperationsList = {
  /**
   * Token to retrieve the next page of results. Omitted when there are no more results.
   */
  nextPageToken?: string | undefined;
  /**
   * The list of tag operations.
   */
  operations: Array<TagOperation> | null;
  /**
   * Total number of operations matching the filters.
   */
  totalSize: number;
};

/** @internal */
export const TagOperationsList$inboundSchema: z.ZodType<
  TagOperationsList,
  z.ZodTypeDef,
  unknown
> = z.object({
  next_page_token: z.string().optional(),
  operations: z.nullable(z.array(TagOperation$inboundSchema)),
  total_size: z.number().int(),
}).transform((v) => {
  return remap$(v, {
    "next_page_token": "nextPageToken",
    "total_size": "totalSize",
  });
});

export function tagOperationsListFromJSON(
  jsonString: string,
): SafeParseResult<TagOperationsList, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TagOperationsList$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TagOperationsList' from JSON`,
  );
}
