/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { Tag, Tag$inboundSchema } from "./tag.js";

export type TagsList = {
  /**
   * Token to retrieve the next page of results. Omitted when there are no more results.
   */
  nextPageToken?: string | undefined;
  /**
   * The list of tags.
   */
  tags: Array<Tag> | null;
  /**
   * Total number of tags visible to the caller in this organization.
   */
  totalSize: number;
};

/** @internal */
export const TagsList$inboundSchema: z.ZodType<
  TagsList,
  z.ZodTypeDef,
  unknown
> = z.object({
  next_page_token: z.string().optional(),
  tags: z.nullable(z.array(Tag$inboundSchema)),
  total_size: z.number().int(),
}).transform((v) => {
  return remap$(v, {
    "next_page_token": "nextPageToken",
    "total_size": "totalSize",
  });
});

export function tagsListFromJSON(
  jsonString: string,
): SafeParseResult<TagsList, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TagsList$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TagsList' from JSON`,
  );
}
