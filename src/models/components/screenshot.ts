/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type Screenshot = {
  extractedText?: string | undefined;
  handle?: string | undefined;
  phash?: string | undefined;
};

/** @internal */
export const Screenshot$inboundSchema: z.ZodType<
  Screenshot,
  z.ZodTypeDef,
  unknown
> = z.object({
  extracted_text: z.string().optional(),
  handle: z.string().optional(),
  phash: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    "extracted_text": "extractedText",
  });
});

export function screenshotFromJSON(
  jsonString: string,
): SafeParseResult<Screenshot, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Screenshot$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Screenshot' from JSON`,
  );
}
