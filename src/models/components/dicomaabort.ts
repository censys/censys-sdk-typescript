/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type DicomAAbort = {
  reasonName?: string | undefined;
  sourceName?: string | undefined;
};

/** @internal */
export const DicomAAbort$inboundSchema: z.ZodType<
  DicomAAbort,
  z.ZodTypeDef,
  unknown
> = z.object({
  reason_name: z.string().optional(),
  source_name: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    "reason_name": "reasonName",
    "source_name": "sourceName",
  });
});

export function dicomAAbortFromJSON(
  jsonString: string,
): SafeParseResult<DicomAAbort, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DicomAAbort$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DicomAAbort' from JSON`,
  );
}
