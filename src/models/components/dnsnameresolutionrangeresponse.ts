/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  DnsResolutionRangeRecord,
  DnsResolutionRangeRecord$inboundSchema,
} from "./dnsresolutionrangerecord.js";

export type DnsNameResolutionRangeResponse = {
  /**
   * The domain name that was queried.
   */
  name: string;
  /**
   * A token that can be used to retrieve the next page of records.
   */
  nextPageToken: string;
  /**
   * The list of DNS observation windows.
   */
  records: Array<DnsResolutionRangeRecord> | null;
  /**
   * The number of records that exist in total across all pages.
   */
  totalRecords: number;
};

/** @internal */
export const DnsNameResolutionRangeResponse$inboundSchema: z.ZodType<
  DnsNameResolutionRangeResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  name: z.string(),
  next_page_token: z.string(),
  records: z.nullable(z.array(DnsResolutionRangeRecord$inboundSchema)),
  total_records: z.number().int(),
}).transform((v) => {
  return remap$(v, {
    "next_page_token": "nextPageToken",
    "total_records": "totalRecords",
  });
});

export function dnsNameResolutionRangeResponseFromJSON(
  jsonString: string,
): SafeParseResult<DnsNameResolutionRangeResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DnsNameResolutionRangeResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DnsNameResolutionRangeResponse' from JSON`,
  );
}
