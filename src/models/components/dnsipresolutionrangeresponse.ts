/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  DnsIpResolutionRangeRecord,
  DnsIpResolutionRangeRecord$inboundSchema,
} from "./dnsipresolutionrangerecord.js";

export type DnsIpResolutionRangeResponse = {
  /**
   * The IP address that was queried.
   */
  ip: string;
  /**
   * A token that can be used to retrieve the next page of records.
   */
  nextPageToken: string;
  /**
   * The list of domain names that resolved to this IP.
   */
  records: Array<DnsIpResolutionRangeRecord> | null;
  /**
   * The number of records that exist in total across all pages.
   */
  totalRecords: number;
};

/** @internal */
export const DnsIpResolutionRangeResponse$inboundSchema: z.ZodType<
  DnsIpResolutionRangeResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  ip: z.string(),
  next_page_token: z.string(),
  records: z.nullable(z.array(DnsIpResolutionRangeRecord$inboundSchema)),
  total_records: z.number().int(),
}).transform((v) => {
  return remap$(v, {
    "next_page_token": "nextPageToken",
    "total_records": "totalRecords",
  });
});

export function dnsIpResolutionRangeResponseFromJSON(
  jsonString: string,
): SafeParseResult<DnsIpResolutionRangeResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DnsIpResolutionRangeResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DnsIpResolutionRangeResponse' from JSON`,
  );
}
