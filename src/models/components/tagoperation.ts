/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import * as openEnums from "../../types/enums.js";
import { OpenEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * The current status of the operation.
 */
export const TagOperationStatus = {
  Pending: "pending",
  Running: "running",
  Succeeded: "succeeded",
  LimitReached: "limit_reached",
  Failed: "failed",
  Cancelled: "cancelled",
} as const;
/**
 * The current status of the operation.
 */
export type TagOperationStatus = OpenEnum<typeof TagOperationStatus>;

/**
 * Whether the operation creates or deletes tag assignments.
 */
export const TagOperationType = {
  BulkCreate: "bulk_create",
  BulkDelete: "bulk_delete",
} as const;
/**
 * Whether the operation creates or deletes tag assignments.
 */
export type TagOperationType = OpenEnum<typeof TagOperationType>;

export type TagOperation = {
  /**
   * RFC3339 timestamp when the operation was created.
   */
  createdAt: Date;
  /**
   * RFC3339 timestamp when the operation completed. Omitted while the operation is still in progress.
   */
  endedAt?: Date | undefined;
  /**
   * Failure detail. Only set when the operation failed.
   */
  errorMessage?: string | undefined;
  /**
   * The unique ID of the operation.
   */
  id: string;
  /**
   * The number of assets that have been processed so far.
   */
  processedCount: number;
  /**
   * The CenQL query used to identify the assets to tag. Only set for bulk create operations.
   */
  query?: string | undefined;
  /**
   * The current status of the operation.
   */
  status: TagOperationStatus;
  /**
   * A human-readable explanation of the operation's outcome, set once the operation finishes — for example, how many assets were assigned, or that the tag's plan asset limit was reached. For failed operations it mirrors error_message.
   */
  statusMessage?: string | undefined;
  /**
   * The number of assets that have been successfully processed so far.
   */
  successfulCount: number;
  /**
   * The ID of the tag the operation applies to.
   */
  tagId: string;
  /**
   * Snapshot of the tag's display name at the time the operation was created.
   */
  tagName: string;
  /**
   * The total number of assets the operation will process. For bulk create operations this is an approximate value set at the start; for bulk delete operations it is set at completion.
   */
  totalCount: number;
  /**
   * Whether the operation creates or deletes tag assignments.
   */
  type: TagOperationType;
};

/** @internal */
export const TagOperationStatus$inboundSchema: z.ZodType<
  TagOperationStatus,
  z.ZodTypeDef,
  unknown
> = openEnums.inboundSchema(TagOperationStatus);

/** @internal */
export const TagOperationType$inboundSchema: z.ZodType<
  TagOperationType,
  z.ZodTypeDef,
  unknown
> = openEnums.inboundSchema(TagOperationType);

/** @internal */
export const TagOperation$inboundSchema: z.ZodType<
  TagOperation,
  z.ZodTypeDef,
  unknown
> = z.object({
  created_at: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  ended_at: z.string().datetime({ offset: true }).transform(v => new Date(v))
    .optional(),
  error_message: z.string().optional(),
  id: z.string(),
  processed_count: z.number().int(),
  query: z.string().optional(),
  status: TagOperationStatus$inboundSchema,
  status_message: z.string().optional(),
  successful_count: z.number().int(),
  tag_id: z.string(),
  tag_name: z.string(),
  total_count: z.number().int(),
  type: TagOperationType$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "created_at": "createdAt",
    "ended_at": "endedAt",
    "error_message": "errorMessage",
    "processed_count": "processedCount",
    "status_message": "statusMessage",
    "successful_count": "successfulCount",
    "tag_id": "tagId",
    "tag_name": "tagName",
    "total_count": "totalCount",
  });
});

export function tagOperationFromJSON(
  jsonString: string,
): SafeParseResult<TagOperation, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TagOperation$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TagOperation' from JSON`,
  );
}
