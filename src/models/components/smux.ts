/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type Smux = {
  closeReasonName?: string | undefined;
};

/** @internal */
export const Smux$inboundSchema: z.ZodType<Smux, z.ZodTypeDef, unknown> = z
  .object({
    close_reason_name: z.string().optional(),
  }).transform((v) => {
    return remap$(v, {
      "close_reason_name": "closeReasonName",
    });
  });

export function smuxFromJSON(
  jsonString: string,
): SafeParseResult<Smux, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Smux$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Smux' from JSON`,
  );
}
