/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type SyncthingBep = {
  deviceName?: string | undefined;
  numConnections?: number | undefined;
  serverName?: string | undefined;
  timestamp?: number | undefined;
  version?: string | undefined;
};

/** @internal */
export const SyncthingBep$inboundSchema: z.ZodType<
  SyncthingBep,
  z.ZodTypeDef,
  unknown
> = z.object({
  device_name: z.string().optional(),
  num_connections: z.number().int().optional(),
  server_name: z.string().optional(),
  timestamp: z.number().int().optional(),
  version: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    "device_name": "deviceName",
    "num_connections": "numConnections",
    "server_name": "serverName",
  });
});

export function syncthingBepFromJSON(
  jsonString: string,
): SafeParseResult<SyncthingBep, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SyncthingBep$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SyncthingBep' from JSON`,
  );
}
