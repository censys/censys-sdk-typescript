/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type Hajime = {
  publicKey?: string | undefined;
};

/** @internal */
export const Hajime$inboundSchema: z.ZodType<Hajime, z.ZodTypeDef, unknown> = z
  .object({
    public_key: z.string().optional(),
  }).transform((v) => {
    return remap$(v, {
      "public_key": "publicKey",
    });
  });

export function hajimeFromJSON(
  jsonString: string,
): SafeParseResult<Hajime, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Hajime$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Hajime' from JSON`,
  );
}
