/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  RtmpConnectStatus,
  RtmpConnectStatus$inboundSchema,
} from "./rtmpconnectstatus.js";

export type Rtmp = {
  capabilities?: number | undefined;
  mode?: number | undefined;
  peerBandwidth?: number | undefined;
  protocolVersion?: number | undefined;
  serverSoftware?: string | undefined;
  status?: RtmpConnectStatus | undefined;
  windowAcknowledgementSize?: number | undefined;
};

/** @internal */
export const Rtmp$inboundSchema: z.ZodType<Rtmp, z.ZodTypeDef, unknown> = z
  .object({
    capabilities: z.number().int().optional(),
    mode: z.number().int().optional(),
    peer_bandwidth: z.number().int().optional(),
    protocol_version: z.number().int().optional(),
    server_software: z.string().optional(),
    status: RtmpConnectStatus$inboundSchema.optional(),
    window_acknowledgement_size: z.number().int().optional(),
  }).transform((v) => {
    return remap$(v, {
      "peer_bandwidth": "peerBandwidth",
      "protocol_version": "protocolVersion",
      "server_software": "serverSoftware",
      "window_acknowledgement_size": "windowAcknowledgementSize",
    });
  });

export function rtmpFromJSON(
  jsonString: string,
): SafeParseResult<Rtmp, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Rtmp$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Rtmp' from JSON`,
  );
}
