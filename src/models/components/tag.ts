/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import * as openEnums from "../../types/enums.js";
import { OpenEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Tag visibility and management settings. `private` tags are only visible to and editable by organization admins. `shared` tags are visible to and editable by all organization members.
 */
export const TagPrivacy = {
  Private: "private",
  Shared: "shared",
} as const;
/**
 * Tag visibility and management settings. `private` tags are only visible to and editable by organization admins. `shared` tags are visible to and editable by all organization members.
 */
export type TagPrivacy = OpenEnum<typeof TagPrivacy>;

export type Tag = {
  /**
   * RFC3339 timestamp when the tag was created.
   */
  createdAt: Date;
  /**
   * The user ID of the user who created the tag.
   */
  createdBy: string;
  /**
   * A human-readable description of the tag.
   */
  description?: string | undefined;
  /**
   * The unique ID of the tag.
   */
  id: string;
  /**
   * The display name of the tag.
   */
  name: string;
  /**
   * Tag visibility and management settings. `private` tags are only visible to and editable by organization admins. `shared` tags are visible to and editable by all organization members.
   */
  privacy: TagPrivacy;
  /**
   * RFC3339 timestamp when the tag was last updated.
   */
  updatedAt: Date;
};

/** @internal */
export const TagPrivacy$inboundSchema: z.ZodType<
  TagPrivacy,
  z.ZodTypeDef,
  unknown
> = openEnums.inboundSchema(TagPrivacy);

/** @internal */
export const Tag$inboundSchema: z.ZodType<Tag, z.ZodTypeDef, unknown> = z
  .object({
    created_at: z.string().datetime({ offset: true }).transform(v =>
      new Date(v)
    ),
    created_by: z.string(),
    description: z.string().optional(),
    id: z.string(),
    name: z.string(),
    privacy: TagPrivacy$inboundSchema,
    updated_at: z.string().datetime({ offset: true }).transform(v =>
      new Date(v)
    ),
  }).transform((v) => {
    return remap$(v, {
      "created_at": "createdAt",
      "created_by": "createdBy",
      "updated_at": "updatedAt",
    });
  });

export function tagFromJSON(
  jsonString: string,
): SafeParseResult<Tag, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Tag$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Tag' from JSON`,
  );
}
