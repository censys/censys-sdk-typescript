/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type SourceUsageBreakdown = {
  /**
   * The amount of credits consumed through the PlatformAPI.
   */
  api: number;
  /**
   * The amount of credits consumed through the Platform UI.
   */
  ui: number;
};

/** @internal */
export const SourceUsageBreakdown$inboundSchema: z.ZodType<
  SourceUsageBreakdown,
  z.ZodTypeDef,
  unknown
> = z.object({
  api: z.number().int(),
  ui: z.number().int(),
});

export function sourceUsageBreakdownFromJSON(
  jsonString: string,
): SafeParseResult<SourceUsageBreakdown, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SourceUsageBreakdown$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SourceUsageBreakdown' from JSON`,
  );
}
