/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import * as openEnums from "../../types/enums.js";
import { OpenEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * The inferred type of the asset.
 */
export const AssetType = {
  Host: "host",
  Certificate: "certificate",
  WebProperty: "web_property",
} as const;
/**
 * The inferred type of the asset.
 */
export type AssetType = OpenEnum<typeof AssetType>;

export type TagAssignment = {
  /**
   * The identifier of the tagged asset (host IP, certificate SHA-256 fingerprint, or web property domain/IP:port).
   */
  assetId: string;
  /**
   * The inferred type of the asset.
   */
  assetType: AssetType;
  /**
   * RFC3339 timestamp when the assignment was created.
   */
  createdAt: Date;
  /**
   * The user ID of the user who created the assignment.
   */
  createdBy: string;
  /**
   * The unique ID of the assignment.
   */
  id: string;
  /**
   * A direct URL to the asset in the Censys platform.
   */
  platformRef: string;
  /**
   * The ID of the tag.
   */
  tagId: string;
};

/** @internal */
export const AssetType$inboundSchema: z.ZodType<
  AssetType,
  z.ZodTypeDef,
  unknown
> = openEnums.inboundSchema(AssetType);

/** @internal */
export const TagAssignment$inboundSchema: z.ZodType<
  TagAssignment,
  z.ZodTypeDef,
  unknown
> = z.object({
  asset_id: z.string(),
  asset_type: AssetType$inboundSchema,
  created_at: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  created_by: z.string(),
  id: z.string(),
  platform_ref: z.string(),
  tag_id: z.string(),
}).transform((v) => {
  return remap$(v, {
    "asset_id": "assetId",
    "asset_type": "assetType",
    "created_at": "createdAt",
    "created_by": "createdBy",
    "platform_ref": "platformRef",
    "tag_id": "tagId",
  });
});

export function tagAssignmentFromJSON(
  jsonString: string,
): SafeParseResult<TagAssignment, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TagAssignment$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TagAssignment' from JSON`,
  );
}
