/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { globalDataGetWebProperty } from "../../funcs/globalDataGetWebProperty.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.V3GlobaldataAssetWebpropertyRequest$inboundSchema,
};

export const tool$globalDataGetWebProperty: ToolDefinition<typeof args> = {
  name: "global-data-get-web-property",
  description: `Asset / WebProperty

Get a WebProperty`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await globalDataGetWebProperty(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
