/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { collectionsGet } from "../../funcs/collectionsGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.V3CollectionsCrudGetRequest$inboundSchema,
};

export const tool$collectionsGet: ToolDefinition<typeof args> = {
  name: "collections-get",
  description: `Get a Collection

Get a Collection`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await collectionsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
