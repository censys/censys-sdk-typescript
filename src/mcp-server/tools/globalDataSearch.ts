/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { globalDataSearch } from "../../funcs/globalDataSearch.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.V3GlobaldataSearchQueryRequest$inboundSchema,
};

export const tool$globalDataSearch: ToolDefinition<typeof args> = {
  name: "global-data-search",
  description: `Search / Query

Search the Global data set`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await globalDataSearch(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
