/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { globalDataGetHosts } from "../../funcs/globalDataGetHosts.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.V3GlobaldataAssetHostListRequest$inboundSchema,
};

export const tool$globalDataGetHosts: ToolDefinition<typeof args> = {
  name: "global-data-get-hosts",
  description: `Asset / Host Bulk

Get multiple Hosts`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await globalDataGetHosts(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
